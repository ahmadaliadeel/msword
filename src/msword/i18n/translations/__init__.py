"""Qt linguist translation sources (``.ts``) and compiled binaries (``.qm``).

The ``.qm`` files are produced by ``pyside6-lrelease`` from the ``.ts``
sources at build time; see :mod:`msword.i18n` for the build invocation.
"""

from __future__ import annotations
